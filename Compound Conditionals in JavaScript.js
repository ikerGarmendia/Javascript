function kid() {
    var age = 15; // Asigna la edad de tu hijo
    
    if (age === 15) {
        return true; // Devuelve true si la edad es exactamente 15
    } else {
        return false; // Devuelve false para otras edades
    }
}
